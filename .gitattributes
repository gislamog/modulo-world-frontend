# Normalise line endings to LF in the repository. Containers and CI run Linux;
# without this, CRLF from Windows checkouts leaks into images and diffs.
* text=auto eol=lf

*.png binary
*.jpg binary
*.svg text eol=lf
