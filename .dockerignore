# Installed inside the image. Copying the host's copy would also carry
# binaries built for the wrong platform.
node_modules

# Build output, regenerated by the build stage.
build
.svelte-kit
coverage

# Never in an image.
.env
.env.*
!.env.example

.git
.github
resources-local-only

.vscode
.DS_Store
Thumbs.db

README.md
