# API base path. Requests are proxied through Nginx on the same origin,
# so this is a path, not an absolute URL. See the architecture doc.
PUBLIC_API_BASE_URL=

# Public site URL, used for canonical links and metadata.
PUBLIC_SITE_URL=
